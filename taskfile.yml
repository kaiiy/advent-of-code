version: 3

silent: true

vars:
  DAY: 3

tasks:
  default:
    cmds:
      - echo "** Day{{.DAY}} **"
      - deno run --allow-read --allow-write ./src/day{{.DAY}}/mod.ts
  test:
    cmds:
      - echo "** Day{{.DAY}} **"
      - deno test --allow-read